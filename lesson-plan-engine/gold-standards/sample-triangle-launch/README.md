# Sample Triangle Launch

Promoted gold-standard lesson-plan bundle for the canonical sample deck.

Benchmark notes:

- `lesson_plan.md`, `lesson_plan.json`, `validation_report.md`, and `lesson_plan.docx` are treated as the approved reference outputs.
- Date fields and the input path field are normalized during benchmark comparison so the check stays deterministic across reruns.
