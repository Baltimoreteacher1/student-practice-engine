# Examples

Store minimal reproducible validation cases here.

Good examples include one passing case, one expected-failure case, and one fidelity-sensitive case per workflow.

Current notebook quality benchmark cases:

- `notebook_session1_volume_case.json`
- `notebook_regular_polygon_fidelity_case.json`
