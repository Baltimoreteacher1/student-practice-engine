# Gold Standards

Store approved final notebook bundles or review-ready references here.

Only keep artifacts that are trusted for layout, fidelity, and production quality.

Current promoted reference bundle:

- `session1-regular-polygon-reference/`

The deterministic benchmark cases in `../../validation-tools/examples/` remain the locked quality floor for the default Session 1 workflow, and the promoted reference bundle is the exact-compare gold standard.
